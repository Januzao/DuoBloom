version: '3.8'
services:
  db:
    image: postgres:15
    container_name: duobloom-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: duobloom
    ports:
      - "6969:5432"